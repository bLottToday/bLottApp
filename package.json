{
  "name": "blott",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "app",
    "packages/*"
  ],
  "main": "index.js",
  "scripts": {
    "api": "cd api && yarn start",
    "app": "cd app && yarn start",
    "build-app": "cd app && yarn build",
    "build-api": "cd api && yarn build"
  },
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
